import { Input } from '../Input'

import S from './style.module.sass'

export const Form = (label, placeholder) => {
    return (
        <>
            <form action="">
                <Input />
            </form>
        </>
    )
}

